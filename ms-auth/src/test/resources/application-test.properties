spring.application.name=hotel-reservation-ms-auth
server.port=8001

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create-drop

# Jwt Configuration
app.jwt.secret=secret_key_for_testing5844=sjuS7d+=
app.jwt.expiration=3600
app.jwt.token-prefix=Bearer